<!-- Generated by gh-inari from .github/inari; do not edit. -->

## Summary

<!-- Concisely describe the delivered result. -->

## Linked issue

Closes #

## Scope

### Included

<!-- Implemented scope. -->

### Excluded

<!-- Explicitly excluded scope. -->

## Implementation

<!-- Key implementation decisions. -->

## Behavioral changes

<!-- User-visible and operational changes. -->

## Validation

- [ ] Typecheck
- [ ] Tests
- [ ] Build
- [ ] Package check

<!-- Keep this completion checklist. It records which repository checks ran;
the structured Validation evidence section records what each check proved. -->

Test layers:

- Fast / unit / contract: `pnpm test`
- Integration / process: `pnpm run test:integration`
- Package / E2E / smoke: `pnpm run test:e2e`, `pnpm run test:package`
- Standards / static: `pnpm run verify:standards`
- Coverage: `pnpm run test:coverage`
- Test effectiveness (when critical logic changes): `pnpm run test:effectiveness`
- Full verification: `pnpm run verify`

## Test contract

<!-- Use one concrete value per field. List every required layer and explain
each Not applicable decision. -->

- change type:
- required layers:
- not-applicable decisions:

## Regression proof

<!-- Bug-fix PRs must provide the regression test path and identifier, the
pre-fix observed failure, and the post-fix result. Automated proof uses only a
trusted fixed runner selected by command id; the validator never executes a
shell command copied from this section. Use reviewer-attested or unsupported
automated proof when the test-diff class is not safely applicable. -->

- status:
- reason:
- test path:
- test identifier:
- pre-fix observed failure:
- post-fix result:
- command id:
- base result:
- head result:
- isolation:
- reviewer attestation:

## Validation evidence

<!-- Add exactly one record for every class below. Canonical record syntax:
- class: unit/contract; status: pass; command: pnpm test; target: path; result: 12 tests passed; artifact: path-or-digest
Use status: not-applicable only when the changed paths do not trigger that
class, and provide a concrete reason. Do not use generic "Tests checked",
TODO, TBD, placeholder, or template-comment-only content. -->

- class: `unit/contract`; status: ; command: ; target: ; result: ; artifact:
- class: `process/integration`; status: ; command: ; target: ; result: ; artifact:
- class: `package smoke`; status: ; command: ; target: ; result: ; artifact:
- class: `fault injection`; status: ; command: ; target: ; result: ; scenario: ; artifact:
- class: `lint/architecture`; status: ; command: ; target: ; result: ; artifact:
- class: `release`; status: ; command: ; target: ; result: ; artifact: ; warnings:
- class: `security/negative`; status: ; command: ; target: ; result: ; scenario: ; artifact:

## Release impact

<!-- State none with a reason when no package or publish path changed. For
release-impacting paths, name the packed artifact, package smoke, and
warning-free packaging or publish evidence. -->

- impact:
- reason:

## Risks

<!-- Risks and mitigations. -->

## Breaking changes

No. <!-- If Yes, describe impact and migration. -->

## Migration / compatibility

<!-- Required for configuration, CLI registration, persisted state, export,
and public tool-schema changes. State the explicit migration decision. -->

## Security impact

<!-- Required for security-related changes. State impact and negative-test
evidence when a security-sensitive path changes. -->

## Review focus

<!-- Specific areas for reviewers to inspect. -->
