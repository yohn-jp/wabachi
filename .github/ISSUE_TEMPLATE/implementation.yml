# Generated by gh-inari from .github/inari; do not edit.
name: Implementation
description: One-session execution contract for work derived from one or more source Issues
title: "impl: "
labels:
  - implementation
body:
  - type: textarea
    id: repository
    attributes:
      label: Repository identity
      description: JSON object containing repositoryHost, repositoryId, and optional owner/name repository locator
    validations:
      required: true
  - type: textarea
    id: sources
    attributes:
      label: Source Issues
      description: One canonical IssueReference JSON object per line
    validations:
      required: true
  - type: textarea
    id: objective
    attributes:
      label: Objective
      description: The concrete outcome for this one implementation session
    validations:
      required: true
  - type: textarea
    id: non_goals
    attributes:
      label: Non-goals
      description: Explicit boundaries; use one item per line
    validations:
      required: true
  - type: textarea
    id: architecture_decision
    attributes:
      label: Architecture decision
      description: The implementation design selected for this contract
    validations:
      required: true
  - type: textarea
    id: affected_components
    attributes:
      label: Affected components
      description: Semantic components or roles affected; one item per line when applicable
    validations:
      required: false
  - type: textarea
    id: invariants
    attributes:
      label: Invariants
      description: Behavioral and architectural truths that must remain true
    validations:
      required: true
  - type: textarea
    id: compatibility_constraints
    attributes:
      label: Compatibility constraints
      description: Compatibility, migration, security, or performance constraints when applicable
    validations:
      required: false
  - type: textarea
    id: readonly_scope
    attributes:
      label: READONLY scope
      description: Readable repository-relative paths or globs; one per line. This never grants WRITE.
    validations:
      required: true
  - type: textarea
    id: write_scope
    attributes:
      label: WRITE scope
      description: Explicit writable repository-relative paths or globs; one per line. Empty means no WRITE.
    validations:
      required: false
  - type: textarea
    id: create_scope
    attributes:
      label: CREATE scope
      description: Paths that may be created; never inferred from WRITE
    validations:
      required: false
  - type: textarea
    id: delete_scope
    attributes:
      label: DELETE scope
      description: Paths that may be deleted; never inferred from WRITE
    validations:
      required: false
  - type: textarea
    id: deny_scope
    attributes:
      label: DENY / exclusions
      description: Explicit exclusions; DENY always overrides broader allowlists
    validations:
      required: false
  - type: textarea
    id: prohibited_operations
    attributes:
      label: Prohibited operations
      description: Operations outside the session contract; one per line
    validations:
      required: false
  - type: textarea
    id: immutable_areas
    attributes:
      label: Immutable areas
      description: Areas that must remain unchanged; one per line
    validations:
      required: false
  - type: textarea
    id: prerequisites
    attributes:
      label: Prerequisites
      description: Dependencies or conditions required before implementation
    validations:
      required: false
  - type: textarea
    id: acceptance_criteria
    attributes:
      label: Acceptance criteria
      description: Verifiable outcomes; one item per line
    validations:
      required: true
  - type: textarea
    id: targeted_tests
    attributes:
      label: Targeted tests
      description: Tests to run or an explicit `none`; one item per line
    validations:
      required: true
  - type: textarea
    id: required_checks
    attributes:
      label: Required checks
      description: Repository checks required before completion or an explicit `none`
    validations:
      required: true
  - type: textarea
    id: postconditions
    attributes:
      label: Observable postconditions
      description: Observable state expected after implementation or an explicit `none`
    validations:
      required: true
  - type: input
    id: base_branch
    attributes:
      label: Base branch
      description: Branch from which the implementation is based
    validations:
      required: true
  - type: input
    id: base_revision
    attributes:
      label: Base revision
      description: Known base commit revision; leave empty when it cannot yet be known
    validations:
      required: false
  - type: textarea
    id: base_freshness
    attributes:
      label: Base freshness
      description: Provider or workflow freshness input, when known
    validations:
      required: false
  - type: input
    id: implementation_branch
    attributes:
      label: Implementation branch
      description: Canonical branch identity when already decided
    validations:
      required: false
  - type: textarea
    id: dependencies
    attributes:
      label: Execution dependencies
      description: One canonical IssueReference JSON object per line when execution depends on other Issues
    validations:
      required: false
